import { ClickSpinDirective } from './click-spin.directive';

describe('ClickSpinDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickSpinDirective();
    expect(directive).toBeTruthy();
  });
});
