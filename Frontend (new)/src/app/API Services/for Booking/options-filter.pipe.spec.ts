import { OptionsFilterPipe } from './options-filter.pipe';

describe('OptionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
